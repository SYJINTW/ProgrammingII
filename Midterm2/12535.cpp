#include <iostream>
#include <iomanip>
#include <map>
#include <vector>
#include <string>
using namespace std;

int main()
{
	int n;
	string str;
	// 第一層：母音出現次數
	// 第二層：最後一個母音相同
	map<int, map<char, vector<string>>> bucket;
	map<char, vector<string>> same_num;
	std::vector<string> same_vowel;
	cin >> n;
	for(int i = 0; i < n; i++){
		
	}

	bucket.insert();
	}	
	return 0;
}